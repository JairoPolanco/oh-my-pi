# research_logs

Benchmark campaign logs for the constitutional kernel harness (branch `kernel-constitutional-hardening`).

Naming: `<component>_<experiment>_<variant>.jsonl` + sibling `.jsonl.json` summaries; seed runs get `_seed<N>`.

Current campaign: `omp_edit_ablation` — baseline OMP vs kernel-harness gates on the TypeScript edit benchmark, agent `opencode-go/deepseek-v4-flash`.
